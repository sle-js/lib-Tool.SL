-- Single qualified name import with that name marked private
use core:Native.Data.Array:1.1.0 import length-
-- js
module.exports = $importAll([
    "core:Native.Data.Array:1.1.0"
]).then($imports => {
    const length =
        $imports[0].length;

    return {};
});
