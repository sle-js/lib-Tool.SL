-- Multiple qualified name import with a mixture of names public and private with renames
use core:Native.Data.Array:1.1.0 import (length, findMap-, append as arrayAppend, prepend as arrayPrepend-)
-- js
module.exports = $importAll([
    "core:Native.Data.Array:1.1.0"
]).then($imports => {
    const length =
        $imports[0].length;

    const findMap =
        $imports[0].findMap;

    const arrayAppend =
        $imports[0].append;

    const arrayPrepend =
        $imports[0].prepend;

    return {
        length,
        arrayAppend
    };
});
