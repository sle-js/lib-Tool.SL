-- Simple import
use core:Native.Data.Array:1.1.0
-- js
module.exports = $importAll([
    "core:Native.Data.Array:1.1.0"
]).then($imports => {
    const Array =
        $imports[0];

    return {
        Array
    };
});
